CREATE TABLE "freelancers" (
	"fre_id" INTEGER NOT NULL UNIQUE GENERATED BY DEFAULT AS IDENTITY,
	"fre_nome" VARCHAR(100) NOT NULL,
	"fre_email" VARCHAR(255) NOT NULL,
	PRIMARY KEY("fre_id")
);

CREATE TABLE "propostas" (
	"pro_id" INTEGER NOT NULL UNIQUE GENERATED BY DEFAULT AS IDENTITY,
	"pro_fre_id" INTEGER,
	PRIMARY KEY("pro_id")
);

ALTER TABLE "freelancers"
ADD FOREIGN KEY("fre_id") REFERENCES "propostas"("pro_fre_id")
ON UPDATE NO ACTION ON DELETE NO ACTION;